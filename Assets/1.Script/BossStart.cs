using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossStart : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //특정 위치에 가면 보스 등장애니메이션 실행
        //애니메이션이 끝나면 보스전 시작
    }
}
